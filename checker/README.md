# Checker Service

The main project documentation is in the repository root: [../README.md](../README.md).

This directory contains the TypeScript Canyon stock checker, its Dockerfile, tests, and pnpm lockfile.
